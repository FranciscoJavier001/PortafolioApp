import React from 'react' //* Importamos React */

export const ProyectosScreen = () => { //* Exportamos la variable de este proyecto */
    return ( //* Lo que voy a retornar */
        <div> {/* Una Etiqueta */}
            <h1>ProyectosScreen</h1> {/* Un Titulo */}
        </div>
    )
}